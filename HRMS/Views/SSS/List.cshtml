@model IEnumerable<HRMS.Models.SSS>

@{
    ViewData["Title"] = "List";
}

<main id="main" class="main">
    <section class="section">
        <h1>List</h1>

        <p>
            <a asp-action="Create">Create New</a>
        </p>
        <form action="List">
            <div class="input-group md-form align-content-sm-around form-2 pl-0">
                <input name="searchValue" class="form-control my-0 py-1 red-border" type="text" placeholder="Search" aria-label="Search" />
                <div class="input-group-append">
                    <button type="submit" class="input-group-text red lighten-3" id="basic-text1">
                        <i class="fas fa-search text-black-grey" aria-hidden="true"></i>
                    </button>
                </div>
            </div>
        </form>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Employee)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SSSNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.StartDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Status)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Employee.FullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SSSNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StartDate)
                    </td>
                    <td>
                       @item.Status
                    </td>
                    <td>
                            <a asp-action="Update" asp-route-SSSId="@item.SSSNumber">Edit</a> |
                            <a asp-action="Details" asp-route-SSSId="@item.SSSNumber">Details</a> |
                            <a asp-action="Delete" asp-route-SSSId="@item.SSSNumber">Delete</a>
                    </td>
                </tr>
        }
            </tbody>
        </table>
    </section>
</main>
@section scripts{
    <script>
        $(function () {
            $("#basic-text1").on('keyup', function (e) {
                $("form").submit();
            });
        });
    </script>
}
